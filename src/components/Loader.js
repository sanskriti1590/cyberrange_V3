import crimeLoader from '../assests/Cyber Crime World .gif'

const Loader = () => {
    return (
        <img src={crimeLoader} alt='' style={{ borderRadius: "100%", border: '4px solid white' }} />
    )
}

export default Loader;